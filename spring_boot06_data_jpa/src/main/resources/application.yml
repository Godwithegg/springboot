spring:
  datasource:
    url: jdbc:mysql://172.21.41.54:3306/jpa
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
  jpa:
    hibernate:
      #更新或者创建数据表，第一次没有数据表就会创建，以后如果再运行会更新
      ddl-auto: update
    #控制台显示sql
    show-sql: true